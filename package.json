{
  "name": "caphne-monorepo",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "dev:client": "pnpm --filter @repo/client dev",
    "dev:server": "pnpm --filter @repo/server dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "typecheck": "turbo typecheck"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  },
  "packageManager": "pnpm@9.15.2",
  "engines": {
    "node": ">=20"
  }
}
